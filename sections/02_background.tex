\section{从经典几何到学习化建模的过渡}

\subsection{传统多视图几何重建范式}

传统多视图几何重建通常建立在 Structure-from-Motion（SfM）与 Multi-View Stereo（MVS）框架之上，其核心流程包括局部特征提取、跨图像匹配、几何验证、相机位姿恢复、三角化以及后续的全局优化\cite{triggs2000bundle,schoenberger2016colmap}。这一范式的优势在于几何解释清晰、优化目标明确，但同时也高度依赖局部描述子的稳定性与初始化质量。只要前端匹配失败，后续相机估计与重建质量往往会快速恶化。

经典管线的另一个特点是模块化程度高。前端负责提取与关联二维观测，后端则负责全局一致性优化。这种分治结构在工程实现上较为成熟，但也使得整个系统难以直接从最终重建误差中反向修正前端表征\cite{wang2024vggsfm}。随着深度学习方法在视觉任务中的广泛成功，研究者开始思考：能否让神经网络承担传统管线中最脆弱或最耗时的部分，同时保留多视图几何的结构性约束。

\subsection{学习方法为何进入多视图几何重建}

学习方法之所以逐步进入多视图几何重建，根本原因在于传统方法在复杂真实场景中的鲁棒性和泛化能力存在明显天花板。面对大视角变化、弱纹理、动态区域与复杂光照时，纯几何启发式往往难以稳定建立高质量对应\cite{wang2021deep2view}。而深度网络则能够从大规模数据中学习跨视角不变特征，并利用上下文提高匹配与估计质量。

不过，多视图几何重建的学习化并不意味着完全放弃几何结构。相反，该方向真正有效的工作通常强调“以几何为骨架、以学习为增强”的思路：学习模块负责估计更可靠的特征、轨迹或初始化，而多视图几何仍然提供问题定义、约束形式与评价标准\cite{bhalgat2023lighttouch,wang2024vggsfm}。从这一角度看，学习方法在该领域的发展更接近“重新组织几何管线”，而非“黑盒替代几何”。

\subsection{与其他三维路线的关系}

为了更准确界定本文主题，有必要把“多视图几何重建中的学习方法”与近年来几条相邻研究路线区分开来。第一类相邻路线是以 NeRF 为代表的神经隐式表示方法\cite{mildenhall2020nerf}。这类方法强调通过连续场函数和体渲染实现高质量新视角合成，在稠密视图条件下具有极强表现，但其核心目标更偏向外观重建与可微渲染，而不一定以显式相机恢复和通用几何输出为中心。

第二类相邻路线是以 3D Gaussian Splatting 为代表的高效显式渲染表示\cite{kerbl2023gaussiansplatting}。相比 NeRF，这类方法在训练和渲染效率上更有优势，也推动了“前馈式三维表示”研究的快速发展。但从问题设定上看，它们多数仍更偏向场景表示与新视角合成，而不是经典多视图几何意义上的结构恢复系统。

第三类相邻路线是近两年兴起的统一几何预测方法，以 DUSt3R 和 MASt3R 为代表\cite{wang2024dust3r,mast3r2024}。这一方向与本文主题关系最为接近，因为它们同样试图弱化传统 SfM 的复杂流程，以统一模型直接恢复几何结构或密集匹配。也正因如此，VGG 团队后续的 VGGT 更值得放在这一背景下理解：它并不是孤立地宣称“前馈更好”，而是在一类统一几何预测方法共同兴起的背景下，沿着 VGG 自身的主线进一步推进。

除此之外，VGG 团队本身也在单图三维重建和生成先验方向上有所探索，例如 RealFusion、PC$^2$ 与 Splatter Image 等工作\cite{melaskyriazi2023realfusion,melaskyriazi2023pc2,szymanowicz2024splatter}。这些方法与本文主线并不完全重合，因为它们更强调单视图条件下的三维表示生成、点云扩散或快速渲染表达，而非多视图几何系统本身；但它们仍然具有分析价值，因为它们说明 VGG 团队在更广泛的三维视觉研究中持续关注一个共同问题，即如何把几何结构压缩到可学习、可复用的中间表示中。正因如此，本文会把这些工作放在“边界与延伸”位置，而不将其作为主线展开。

\subsection{VGG 团队早期代表工作}

VGG 团队的早期工作体现出鲜明的过渡特征。首先，\textit{Unsupervised Learning of Probably Symmetric Deformable 3D Objects from Images in the Wild} 关注的是在弱监督条件下利用对称性等物理先验恢复三维结构，体现了“先验驱动学习”的思路\cite{wu2020unsup3d}。该工作虽然更接近单图重建，但它说明了几何与形状先验在学习式三维理解中的基础作用。

其次，\textit{Deep Two-View Structure-from-Motion Revisited} 则更加直接地讨论双视图结构恢复中哪些环节适合学习、哪些环节应保留经典几何求解\cite{wang2021deep2view}。该工作并未简单回归相机位姿或深度，而是将光流估计、相对位姿恢复和尺度不变深度估计有机组合，强调经典几何问题的可解性边界。这类工作为后续更系统的可微 SfM 研究奠定了方法论基础。

再次，\textit{Common Objects in 3D}（CO3D）数据集工作则从数据层面补齐了真实世界多视图学习的关键基础设施\cite{reizenstein2021co3d}。由于许多学习方法长期依赖合成数据或小规模真实数据，跨域泛化能力受限，CO3D 提供的大规模真实多视图对象数据集为后续研究提供了更接近真实场景的训练与评测基准。

如果把这些工作放在统一视角下观察，可以发现 VGG 团队的早期过渡期其实围绕三个基本判断展开。第一，纯粹依赖黑盒回归难以充分利用多视图几何中的可解结构。第二，若没有足够真实的数据支撑，再强的模型也很难在真实场景中稳定泛化。第三，几何先验并不是束缚学习的限制条件，反而可以作为提升学习效率和推理可靠性的关键归纳偏置。正是基于这三点判断，后续关于 Transformer 注意力、点轨迹和可微 SfM 的工作才具备清晰的方法论延续性。

若进一步按照课程要求去辨析这些代表成果之间的关系，可以看到它们并不是简单并列的几篇“早期论文”。其中，Probably Symmetric 主要承担“先验启发”的作用，说明学习式三维理解为什么仍需要物理归纳偏置；CO3D 主要承担“数据基础设施”的作用，解决真实多视图训练与评测的前提条件；Deep Two-View 则更像方法论上的过渡节点，它明确讨论了哪些部分适合学习、哪些部分应保留经典几何求解。因此，这一阶段更准确的理解不是“VGG 在不同方向上同时做了几件事”，而是“VGG 从问题设定、数据支撑和方法边界三个层面，为后续主线搭好了起点”。

从课程综述写作角度看，这一阶段还有一个容易被忽略、但非常关键的意义：它为后文建立了“为什么 VGG 主线值得单独分析”的逻辑基础。许多团队也在做三维重建或视觉几何，但并不是所有研究都同时覆盖问题设定、训练数据、观测形式与系统求解四个层面。VGG 团队早期工作的价值，恰恰在于它们已经同时触及这四个层面，从而使后续工作能够沿着一条连贯主线展开，而不是在不同任务之间跳跃。

\begin{figure}[H]
  \centering
  \includegraphics[width=11.2cm]{figures/vgg_timeline.png}
  \BiCaption{VGG 团队多视图几何学习主线时间轴（2020--2025）}{Timeline of the VGG Research Line in Learning-based Multi-view Geometry (2020--2025)}
  \label{fig:vgg-timeline}
\end{figure}

图~\ref{fig:vgg-timeline} 对本文所讨论的主线进行了时间维度上的集中展示。可以看到，VGG 团队并不是从一开始就直接提出统一前馈式几何模型，而是先后经历了几何先验与数据基础、对应学习与系统重构、统一预测与动态扩展三个阶段。这种“由浅入深、由局部到系统”的推进节奏，正是本文选择按研究主线而非按论文年份简单罗列来组织综述的重要原因。

\subsection{本综述的分析视角}

基于上述背景，本文后续不按论文年份简单罗列，而是按照学习方法逐步介入传统多视图几何管线的方式来组织。具体而言，本文将从“几何对应学习”“可微结构恢复”“统一前馈预测”三个层面梳理 VGG 团队的代表工作，并在必要处引入 DUSt3R 等相关方法作为横向对照\cite{wang2024dust3r}。

这一分析视角的优点在于，它能更好回答课程综述中最重要的问题：该研究方向主要关注什么问题、采用了哪些方法、研究思路是如何逐步推进的。换言之，本文不会把 VGG 团队的论文写成平行罗列，而会强调它们之间在问题设置、方法设计和研究目标上的前后联系，尤其关注每一步工作是如何针对上一阶段的不足提出修正或扩展的。

更具体地说，本文把 VGG 团队近五年的长期核心问题概括为同一条问题链：如何在不丢失显式几何结构的前提下，提高观测鲁棒性、重组系统求解流程，并最终形成可统一输出、可跨任务迁移的场景几何表示。围绕这条问题链，第三章主要讨论观测层与表征层的改造，其中 Light Touch 属于几何先验前移，CoTracker 系列属于对观测形式的递进改进，SHIC 属于相关方向的拓展；第四章讨论系统层重构，其中 VGGSfM 是承上启下的枢纽；第五章则讨论表示层与输出层的统一，其中 VGGT 是阶段性推进，Dynamic Point Maps 是向动态场景的延伸，而 DUSt3R、MASt3R 等工作主要作为横向对照，用于帮助定位 VGG 主线在整个方向中的独特性。这样的组织方式既属于“按研究内容分类”，也兼顾了“按时间发展梳理”的要求。
